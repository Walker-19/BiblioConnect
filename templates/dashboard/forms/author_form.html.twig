{{ form_start(form, {'attr': {'id': 'form-author'}, 'action': path('admin_author_add')}) }}
    {{ form_row(form.prenom) }}
    {{ form_row(form.nom) }}
    {{ form_rest(form) }}
    {{ form_errors(form) }}
{{ form_end(form) }}
